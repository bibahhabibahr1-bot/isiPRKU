{
  "name": "isipr-app",
  "version": "1.0.0",
  "private": true,
  "description": "isiPR - asisten belajar untuk siswa, dibungkus jadi aplikasi Android dengan Capacitor",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
